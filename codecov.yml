coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

ignore:
  - "cmd/blockyard/**"              # main() server wiring — not unit-testable
  - "internal/testutil/**"          # test helpers only
