coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

ignore:
  - "cmd/**"                        # main() wiring — not unit-testable
  - "internal/testutil/**"          # test helpers only
