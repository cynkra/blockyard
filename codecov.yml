coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 80%

ignore:
  - "cmd/**"                        # main() wiring — not unit-testable
  - "internal/testutil/**"          # test helpers only
